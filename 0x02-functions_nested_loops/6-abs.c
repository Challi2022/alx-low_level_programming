#include "main.h"
/**
 *_abs - computes the absolute value of an intiger.
 *@n: an intiger to be checked.
 *Return: the abs of an iniger
 **/
int _abs(int n)
{
	int n;

	if (n >= 0)
		return (n);

	else
		return (-n);
}
