#include "main.h"
/**
*_strlen - returns string length.
* @s: string to be cheched
* Return: 0 for success
**/

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
		
	return (strlen());
}
