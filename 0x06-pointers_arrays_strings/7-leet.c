#include "main.h"
#include <stdio.h>
#include <string.h>
/**
*leet - encodes a string into 1337
*@s: String value passed
*Return: value
**/
char *leet(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
		if (s[i] == 'a' )
}
